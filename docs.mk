# List of top-level TeX documents (no paths, just basenames without .tex if you want)
# Each of these must exist as <name>.tex in the repo root.
# Example:
# DOCUMENTS := cv motivation_letter
#
# By default we only build main.tex -> main.pdf:
DOCUMENTS := main
